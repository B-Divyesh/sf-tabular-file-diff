# Key-aware diff for CSV/Parquet/Arrow files with git and DVC integration

Live: https://tabular-file-diff.sociobot.in — built by the Param Factory (`library-pypi`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
