[
  {
    "id": "demo-one-click",
    "claim": "The sample demo immediately shows added, removed, changed, and schema results.",
    "where": "home action and /demo",
    "test": "npm run test:claims -- --grep @claim:demo-one-click",
    "sandbox": "fresh browser context opens /demo/"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo state is isolated under demo: and Start for real removes it.",
    "where": "/demo banner and privacy page",
    "test": "npm run test:claims -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser context, Reset demo, then Start for real"
  },
  {
    "id": "browser-private",
    "claim": "The browser preview sends no request beyond this origin.",
    "where": "/demo and privacy page",
    "test": "npm run test:claims -- --grep @claim:browser-private",
    "sandbox": "request interception over direct demo flow"
  },
  {
    "id": "no-account",
    "claim": "The sample demo needs no account.",
    "where": "landing facts",
    "test": "npm run test:claims -- --grep @claim:no-account",
    "sandbox": "fresh browser context opens /demo/ and checks the visible flow"
  },
  {
    "id": "offline-demo",
    "claim": "The demo reloads offline after its first visit.",
    "where": "offline notice",
    "test": "npm run test:claims -- --grep @claim:offline-demo",
    "sandbox": "controlled service-worker client then browser context offline"
  },
  {
    "id": "package-formats",
    "claim": "tdiff compares keyed CSV, Parquet, and Arrow data files.",
    "where": "home, README",
    "test": "python3 -m pytest tests/test_claims.py -k claim_package_formats",
    "sandbox": "temporary files for each supported format through CLI JSON output"
  },
  {
    "id": "html-report",
    "claim": "The CLI writes a self-contained HTML report.",
    "where": "home, README",
    "test": "python3 -m pytest tests/test_claims.py -k claim_html_report",
    "sandbox": "temporary CSV inputs and generated report"
  },
  {
    "id": "git-wrapper",
    "claim": "tdiff-git lets git diff print changed keyed CSV files without an external-driver error.",
    "where": "README",
    "test": "python3 -m pytest tests/test_claims.py -k claim_git_wrapper",
    "sandbox": "temporary Git repository and changed CSV"
  },
  {
    "id": "dvc-wrapper",
    "claim": "tdiff-dvc compares a materialized DVC revision with the workspace.",
    "where": "README",
    "test": "python3 -m pytest tests/test_claims.py -k claim_dvc_wrapper",
    "sandbox": "temporary DVC adapter materialization boundary"
  },
  {
    "id": "mit-license",
    "claim": "tdiff is free and MIT licensed.",
    "where": "home, README",
    "test": "python3 -m pytest tests/test_claims.py -k claim_mit_license",
    "sandbox": "repository license file"
  },
  {
    "id": "local-no-telemetry",
    "claim": "Local package comparisons make no network connection or telemetry request.",
    "where": "README, privacy page",
    "test": "python3 -m pytest tests/test_claims.py -k claim_local_no_telemetry",
    "sandbox": "temporary local CSV comparison with socket creation blocked"
  },
  {
    "id": "cli-contract",
    "claim": "tdiff reports keyed changes and the Python API returns Arrow tables.",
    "where": "README",
    "test": "python3 -m pytest tests/test_claims.py -k claim_cli_contract_and_python_api",
    "sandbox": "temporary CSV inputs through CLI JSON and Python API"
  },
  {
    "id": "packaged-demo",
    "claim": "tdiff demo runs the bundled sample and writes a report in a temporary directory.",
    "where": "home, README",
    "test": "python3 -m pytest tests/test_claims.py -k claim_packaged_demo",
    "sandbox": "installed package sample command"
  }
]
